bundle:
  name: migration-accelerator

include:
  - tags.yml
  - resources/*.yml  

targets:
  dev:
    default: true
    presets:
      tags: ${var.global_project_tag}
    vars:
      DDL_LOG_LEVEL: "INFO"