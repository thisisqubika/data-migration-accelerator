bundle:
  name: migration-accelerator

include:
  - tags.yml
  - resources/*.yml  
  - databricks_job_executor/app.yml 

targets:
  dev:
    default: true
    presets:
      tags: ${var.global_project_tag}
    vars:
      DDL_LOG_LEVEL: "INFO"