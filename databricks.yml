bundle:
  name: migration-accelerator

include:
  - tags.yml
  - resources/*.yml   

targets:
  dev:
    default: true
    presets:
      tags: ${var.global_project_tag}
    vars:
      DDL_LOG_LEVEL: "DEBUG"
      DDL_LOG_FILE: "/Volumes/qubika_partner_solutions/migration_accelerator/logs/translation_graph.log"